name: Publish package for Production

on:
  push:
    tags:
      - '*'

jobs:
  publish-github-packages:
    name: Publish on registry
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Get tag
        id: get_tag
        run: echo ::set-output name=TAG::$(echo $GITHUB_REF | cut -d / -f 3)

      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8.x.x

      - name: Setup node env 🏗
        uses: actions/setup-node@v3
        with:
          node-version: lts/hydrogen
          registry-url: https://registry.npmjs.org/
          cache: 'pnpm'

      - name: Install dependencies 👨🏻‍💻
        run: pnpm i --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Update npm version
        run:  pnpm -r --parallel --filter './packages/**' exec pnpm version $VERSION --no-git-tag-version --allow-same-version
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          VERSION: ${{ steps.get_tag.outputs.TAG }}

      - name: Publish
        run: |
          if [[ $VERSION =~ beta$ ]]; then
            pnpm -r --parallel --filter './packages/**' exec pnpm publish --no-git-checks --access public --tag "beta"
          else 
            pnpm -r --parallel --filter './packages/**' exec pnpm publish --no-git-checks --access public
          fi

        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          VERSION: ${{ steps.get_tag.outputs.TAG }}
